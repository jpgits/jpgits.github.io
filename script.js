document.addEventListener('DOMContentLoaded', () => {
  const heading = document.querySelector('header > h1');
  heading.addEventListener('click', () => {
    alert('ようこそ・ポートフォリオサイトへ、ごみです。');
  });
});
